import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SignInScreen = () => <Text style={{ fontSize: 48 }}>SignInScreen</Text>;

const styles = StyleSheet.create({});

export default SignInScreen;
